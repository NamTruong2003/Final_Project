package com.healthtracker.model

enum class Goal(val description: String) {
    LOSE_WEIGHT(description = "Giảm cân"),
    MAINTAIN_WEIGHT(description = "Giữ cân"),
    GAIN_WEIGHT(description = "Tăng cân");
}
